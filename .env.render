# Environment variables for Render.com deployment
# Copy these to Render's environment variables section

# === Database Configuration ===
# Use Render's Internal Database URL (auto-provided when you add PostgreSQL)
# Format: postgresql://user:password@hostname/database
DATABASE_URL=<YOUR_RENDER_POSTGRES_INTERNAL_URL>

# === LLM Provider Configuration ===
LLM_PROVIDER=GEMINI
GEMINI_API_KEY=<YOUR_GEMINI_API_KEY>

# === Free Tier Optimizations ===
# Reduce resource usage for 512 MB RAM limit
MAX_WORKERS=2
MAX_CONCURRENT_JOBS=2

# === Production Settings ===
ENVIRONMENT=production
