# Complete System Architecture

## 🎯 Full Integration Map

```
┌─────────────────────────────────────────────────────────────────────┐
│                         USER INTERFACES                             │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────────────────┐    ┌──────────────────────────┐     │
│  │   NEW DASHBOARD UI       │    │   OLD EDITOR UI          │     │
│  │   (dashboard.html)       │    │   (index.html)           │     │
│  │   http://localhost:8000/ │    │   /old-editor            │     │
│  ├──────────────────────────┤    ├──────────────────────────┤     │
│  │ • Login/Register         │    │ • Manual Form Builder    │     │
│  │ • User Stats Display     │    │ • JSON Upload/Download   │     │
│  │ • Resume Generation      │    │ • Direct Generation      │     │
│  │ • Job History Tracker    │    │ • No Authentication      │     │
│  │ • Progress Bars          │    │                          │     │
│  │ • Download Buttons       │    │                          │     │
│  │ • Real-time Polling      │    │                          │     │
│  └──────────┬───────────────┘    └──────────┬───────────────┘     │
│             │                               │                      │
└─────────────┼───────────────────────────────┼──────────────────────┘
              │                               │
              │    HTTPS/JSON API Calls       │
              │    (with Basic Auth)          │
              │                               │
              ▼                               ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      FASTAPI BACKEND                                │
│                      (app/main.py)                                  │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────────────────────────────────────────────────┐      │
│  │              MIDDLEWARE LAYER                           │      │
│  ├─────────────────────────────────────────────────────────┤      │
│  │ • CORS (allow_origins=*)                                │      │
│  │ • Rate Limiter (5 req/min per IP)                       │      │
│  │ • HTTP Basic Auth                                        │      │
│  └─────────────────────────────────────────────────────────┘      │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────┐      │
│  │              API ENDPOINTS                              │      │
│  ├─────────────────────────────────────────────────────────┤      │
│  │ Authentication:                                          │      │
│  │   POST /api/auth/register                               │      │
│  │   POST /api/auth/login                                  │      │
│  │                                                          │      │
│  │ Resume Generation:                                       │      │
│  │   POST /api/generate_resume_json  [Rate Limited]        │      │
│  │         ↓ Creates background task                        │      │
│  │         ↓ Updates progress (0→100%)                      │      │
│  │                                                          │      │
│  │ Job Management:                                          │      │
│  │   GET  /api/jobs/{id}/status                            │      │
│  │   GET  /api/jobs/{id}/result                            │      │
│  │   GET  /api/jobs/{id}/download                          │      │
│  │                                                          │      │
│  │ User Management:                                         │      │
│  │   GET  /api/user/jobs                                   │      │
│  │   GET  /api/user/stats                                  │      │
│  │                                                          │      │
│  │ Health:                                                  │      │
│  │   GET  /health                                          │      │
│  └─────────────────────────────────────────────────────────┘      │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────┐      │
│  │           BUSINESS LOGIC LAYER                          │      │
│  ├─────────────────────────────────────────────────────────┤      │
│  │ • Usage Limit Checks:                                   │      │
│  │   - Max 3 concurrent jobs per user                      │      │
│  │   - Max 20 resumes per day per user                     │      │
│  │                                                          │      │
│  │ • Background Processing:                                │      │
│  │   - Extract JD hints (5%)                               │      │
│  │   - Score resume (10%)                                  │      │
│  │   - Generate sections (40-75%)                          │      │
│  │   - Finalize (95-100%)                                  │      │
│  │                                                          │      │
│  │ • DOCX Generation (on-the-fly)                          │      │
│  └─────────────────────────────────────────────────────────┘      │
│                             │                                       │
└─────────────────────────────┼───────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│                       DATABASE LAYER                                │
│                       (SQLAlchemy ORM)                              │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────────────┐         ┌──────────────────────┐        │
│  │   Users Table        │         │   ResumeJob Table    │        │
│  ├──────────────────────┤         ├──────────────────────┤        │
│  │ • id (PK)            │         │ • id (PK)            │        │
│  │ • user_id            │◄────┐   │ • user_id (FK)       │        │
│  │ • password_hash      │     └───┤ • request_id         │        │
│  │ • total_resumes      │         │ • company_name       │        │
│  │ • active_jobs_count  │         │ • job_title          │        │
│  │ • created_at         │         │ • mode               │        │
│  │ • last_login         │         │ • jd_text            │        │
│  │ • is_active          │         │ • resume_input_json  │        │
│  └──────────────────────┘         │ • final_resume_json  │        │
│                                   │ • status             │        │
│                                   │ • progress (0-100)   │        │
│                                   │ • error_message      │        │
│                                   │ • created_at         │        │
│                                   │ • completed_at       │        │
│                                   └──────────────────────┘        │
│                                                                     │
│  Database Type: PostgreSQL (production) / SQLite (local)           │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      EXTERNAL SERVICES                              │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────────────┐         ┌──────────────────────┐        │
│  │   OpenAI API         │         │   File System        │        │
│  ├──────────────────────┤         ├──────────────────────┤        │
│  │ • GPT-4 for resume   │         │ • debug_files/       │        │
│  │   generation         │         │ • generated_resumes/ │        │
│  │ • Prompt execution   │         │ • Temp DOCX files    │        │
│  └──────────────────────┘         └──────────────────────┘        │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

## 🔄 Data Flow Example: Generate Resume

```
1. User fills form in Dashboard UI
   ↓
2. Click "Generate Resume" button
   ↓
3. JavaScript: handleGenerateResume(event)
   ↓
4. API Call: POST /api/generate_resume_json
   {
     company_name, job_title, mode, jd, resume_data
   }
   ↓
5. Backend: Check rate limits & quotas
   ├─ Rate limit: 5/min per IP ✓
   ├─ Active jobs: < 3 per user ✓
   └─ Daily quota: < 20 per user ✓
   ↓
6. Create ResumeJob record in database
   status: 'pending', progress: 0
   ↓
7. Start background task: process_resume_background()
   ↓
8. Update progress in database:
   - Extract JD hints → 5%
   - Score resume → 10%
   - Generate summary → 40%
   - Generate experience → 60%
   - Generate skills → 75%
   - Finalize → 95%
   - Complete → 100%
   ↓
9. Frontend: Auto-poll every 2 seconds
   GET /api/jobs/{request_id}/status
   ↓
10. Update progress bar in UI
    ↓
11. When status='completed':
    - Show "Download DOCX" button
    - Stop polling
    - Refresh user stats
    ↓
12. User clicks "Download DOCX"
    ↓
13. API Call: GET /api/jobs/{request_id}/download
    ↓
14. Backend generates DOCX on-the-fly
    (no file storage, streams directly)
    ↓
15. Browser downloads resume.docx
```

## 🔐 Authentication Flow

```
1. User visits http://localhost:8000/
   ↓
2. Dashboard shows Login/Register tabs
   ↓
3. User clicks "Register"
   ↓
4. POST /api/auth/register
   { user_id: "john", password: "secret123" }
   ↓
5. Backend: Hash password with PBKDF2-SHA256
   ↓
6. Create User in database
   ↓
7. Return success message
   ↓
8. User switches to Login tab
   ↓
9. POST /api/auth/login (with credentials)
   ↓
10. Backend: Verify password hash
    ↓
11. Return success
    ↓
12. Frontend: Store credentials in memory
    ↓
13. All subsequent API calls include:
    Authorization: Basic base64(username:password)
    ↓
14. Backend: Validate on each request via get_current_user()
```

## 📊 Usage Limits Enforcement

```
┌─────────────────────────────────────────────────────────┐
│         Request to /api/generate_resume_json            │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
         ┌───────────────────────┐
         │ Rate Limiter Check    │
         │ (SlowAPI)             │
         ├───────────────────────┤
         │ 5 requests/minute     │
         │ per IP address        │
         └───────┬───────────────┘
                 │ ✓ Pass
                 ▼
         ┌───────────────────────┐
         │ Active Jobs Check     │
         ├───────────────────────┤
         │ Query database:       │
         │ WHERE user_id = X     │
         │   AND status IN       │
         │   ('pending',         │
         │    'processing')      │
         │                       │
         │ Count < 3?            │
         └───────┬───────────────┘
                 │ ✓ Pass
                 ▼
         ┌───────────────────────┐
         │ Daily Quota Check     │
         ├───────────────────────┤
         │ Query database:       │
         │ WHERE user_id = X     │
         │   AND created_at      │
         │       >= today 00:00  │
         │   AND status =        │
         │       'completed'     │
         │                       │
         │ Count < 20?           │
         └───────┬───────────────┘
                 │ ✓ Pass
                 ▼
         ┌───────────────────────┐
         │ Create Job            │
         │ Start Processing      │
         └───────────────────────┘
```

## ✅ **YES, Everything is Interlinked!**

**Summary:**
1. ✅ **Dashboard UI** → Calls backend API endpoints
2. ✅ **Old Editor UI** → Calls same backend API endpoints
3. ✅ **Backend API** → Manages database, authentication, rate limiting
4. ✅ **Database** → Stores users, jobs, tracks usage
5. ✅ **Rate Limiting** → Enforced on all generation requests
6. ✅ **Progress Tracking** → Real-time updates via polling
7. ✅ **Download** → On-the-fly DOCX generation
8. ✅ **History** → Query database for user's past jobs
9. ✅ **Stats** → Calculate from database records

**All features work together seamlessly!** 🚀
